# Jutsu Audio project guidance

See [CLAUDE.md](CLAUDE.md) — it is the single source for commands, architecture, invariants,
and the `tasks/` tracker conventions. This file exists so agents that look for `AGENTS.md`
find their way there instead of duplicating rules that would drift.

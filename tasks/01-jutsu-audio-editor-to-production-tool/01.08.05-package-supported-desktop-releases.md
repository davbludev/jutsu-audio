---
id: 01.08.05
title: Package supported desktop releases
status: done
type: chore
priority: medium
depends: [01.08.04]
started: 2026-08-19
completed: 2026-08-19
---

Create reproducible signed-ready builds, first-run audio setup, CLI installation/path integration, license notices, and upgrade/uninstall behavior.

## Acceptance

Clean-machine smoke tests pass on each declared platform.

## Notes

- 2026-08-19: `cargo package-release` builds a release directory — both binaries, the CLI and SDK
  docs, generated `INSTALL.md`, `THIRD-PARTY-NOTICES.md` (from `cargo metadata`, grouped by
  license, undeclared licenses surfaced) and `SHA256SUMS`. Reproducible by construction:
  `--locked`, a pinned toolchain, sorted iteration, nothing dated in any artifact. `cargo smoke
  <dir>` runs the packaged binaries the way a user would — `--version`, discovery, create,
  generate, export — and touches nothing in the repository. The CLI gained `--version`/`--help`
  for PATH integration, and the GUI a first-run notice when no output device opens, with a retry.
  Process, declared platforms, signing steps and the by-hand checklist: `docs/release.md`.
- 2026-08-19: Verified on `x86_64-pc-windows-msvc` only — packaged and smoke-tested there. The
  macOS and Linux targets in `docs/release.md` still need their first run on those machines.

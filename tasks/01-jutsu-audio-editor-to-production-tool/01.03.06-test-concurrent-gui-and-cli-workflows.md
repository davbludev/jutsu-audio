---
id: 01.03.06
title: Test concurrent GUI and CLI workflows
status: done
type: task
priority: medium
depends: [01.03.05]
started: 2026-08-19
completed: 2026-08-19
---

Automate live attach, rapid edits during playback, stale revisions, duplicate requests, disconnects, GUI crash, and offline fallback.

## Acceptance

No lost updates, deadlocks, partial writes, or unstructured failures.

## Notes

- 2026-08-19: `tests/session_workflows.rs` stands up a real host over a real socket and drives it
  with the real CLI handler in-process: attach, a burst of concurrent edits, stale revision,
  replayed request ID, mid-session disconnect, a crashed editor's stale session file, and two
  offline writers on the lock. `SessionHost` moved into the library so a test can own one the way
  the shell does.
- 2026-08-19: audio during those edits is not asserted here — real-time and offline render parity
  already has its own test in `jutsu-audio-engine`, and the editor's re-mix path is UI-thread glue
  with no session state in it.

---
id: 01.08.03
title: Complete machine-friendly headless workflows
status: done
type: task
priority: medium
depends: [01.08.02]
started: 2026-08-19
completed: 2026-08-19
---

Add capability/schema discovery, batch command transactions, dry-run, deterministic status/events, progress, cancellation, and JSONL where streaming helps.

## Acceptance

Representative Codex workflow needs no GUI scraping or ambiguous text.

## Notes

- 2026-08-19: Added `describe_protocol` (operations with summaries, protocol and schema versions,
  exit codes, where to ask for extension-dependent capability) and `batch` (ordinary requests run
  as one all-or-nothing change, with `dry_run`, JSONL `progress` on stderr, and `timeout_ms` as
  the cancellation path), both in `src/cli_batch.rs`. A batch refuses to run behind a live editor
  rather than restoring the file underneath it. `tests/headless_workflows.rs` walks a script from
  discovery through a generated cue to an export, and pins the operation list against what the
  request enum actually accepts so discovery cannot drift.

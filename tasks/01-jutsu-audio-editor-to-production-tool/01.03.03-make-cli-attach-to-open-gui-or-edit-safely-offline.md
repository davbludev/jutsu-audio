---
id: 01.03.03
title: Make CLI attach to open GUI or edit safely offline
status: done
type: task
priority: medium
depends: [01.03.02]
started: 2026-08-19
completed: 2026-08-19
---

Detect live session, send mutations over IPC, otherwise acquire project lock and apply atomically on disk.

## Acceptance

CLI never races GUI file writes and reports connection/revision status in JSON.

## Notes

- 2026-08-19: `src/cli_session.rs` is the one place that picks a route — live session first,
  otherwise the write lock held across the whole read-modify-write. Every mutating response now
  carries `delivery` and `revision`, a new `session_status` operation reports the owner, and
  exit code `5` covers `session_unavailable` / `project_locked` / `revision_conflict`.
  `transport_request` gained a required `path` so it can reach the live editor.
